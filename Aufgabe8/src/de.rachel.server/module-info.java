/// That module that serves a webservice on localhost
/// where you can upload a csv and these will
/// represented in a html table
module de.rachel.server {
  requires de.rachel.lib;
  requires jdk.httpserver;
}
